[build]
  command = "hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.119.0"
  HUGO_ENABLEGITINFO = "true"
  GO_VERSION = "1.21"

[context.production.environment]
  HUGO_ENV = "production"
